"""Archived retired runtime service-locator module.

Runtime dependencies are assembled in ``ApplicationRuntime`` and passed explicitly.
Task execution state lives in ``backend.core.task_runtime``.
"""
