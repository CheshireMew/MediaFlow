# Removed preprocessing feature

This archive contains the standalone preprocessing UI and its exclusive OCR,
overlay-cleaning, enhancement task, tests, scripts, and visual baseline removed
from the active MediaFlow application.

The later `super-resolution/` addition contains the synthesis pipeline's
retired RealESRGAN and BasicVSR implementation, model helpers, and maintenance
scripts. No preprocessing or AI super-resolution capability remains active.
