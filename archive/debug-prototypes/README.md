# Archived debug prototypes

`debug_snapshot.py` was moved here on 2026-07-10. It referenced an undefined
global task manager and the retired Pydantic v1 `.dict()` serialization path,
so it could not run against the current application architecture.

The script is retained for historical context and is outside active lint and
runtime boundaries.
