# Third-party notices

MediaFlow integrates or may invoke components such as Electron, FFmpeg, yt-dlp, speech-recognition models, transcription engines, and other package-manager dependencies. Each component remains under its own license.

Model weights and binaries downloaded separately are not distributed under MediaFlow's GPL merely because the application can use them. Preserve all license and attribution files shipped with each dependency.
